#!/bin/bash
# don't worry this is just a simple helper script to test
# the program from the source tree. The real execution
# script is handled by the console_scripts entry point in
# setup file
#
pushd ../ &>/dev/null

./.version > schema/kiwi.revision

python -c "from kiwi.kiwi import main; main()" "$@"
