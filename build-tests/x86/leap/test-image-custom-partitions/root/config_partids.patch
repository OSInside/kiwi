--- /usr/lib/dracut/modules.d/55kiwi-repart/kiwi-repart-disk.sh
+++ /usr/lib/dracut/modules.d/55kiwi-repart/kiwi-repart-disk.sh
@@ -43,6 +43,8 @@ function initialize {
     else
         import_file ${partition_ids}
 
+        export kiwi_RootPart=7
+
         disk=$(lookup_disk_device_from_root)
         export disk
     fi
